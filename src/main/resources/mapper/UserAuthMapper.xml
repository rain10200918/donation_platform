<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.donationplatform.mapper.UserAuthMapper">
    <insert id="insert" parameterType="com.example.donationplatform.entity.UserAuth">
        INSERT INTO user_auth (user_id, real_name, id_number, auth_status)
        VALUES (#{userId}, #{realName}, #{idNumber}, #{authStatus})
    </insert>

    <update id="updateById" parameterType="com.example.donationplatform.entity.UserAuth">
        UPDATE user_auth
        SET real_name = #{realName},
            id_number = #{idNumber},
            auth_status = #{authStatus},
            reject_reason = #{rejectReason}
        WHERE id = #{id}
    </update>
</mapper>